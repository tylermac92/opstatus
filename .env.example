# Required
# For local development with docker-compose:
DATABASE_URL=postgresql+asyncpg://opstatus:opstatus@postgres:5432/opstatus
# For local development without docker (SQLite):
# DATABASE_URL=sqlite+aiosqlite:///./opstatus.db

# Optional — defaults shown
LOG_LEVEL=INFO
APP_ENV=development
API_HOST=0.0.0.0
API_PORT=8000